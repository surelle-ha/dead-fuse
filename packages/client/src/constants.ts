/**
 * Default dashboard URL used when no explicit `master` value is provided.
 * Replace this with your deployed dashboard host if you want a different endpoint.
 */
export const DEFAULT_MASTER = "https://dead-fuse.surelle.xyz";
